heapsize = heapsize or 12512
h1 = assert(init(heapsize))
p1 = assert(alloc(h1, 0x7f8))
p2 = assert(alloc(h1, 0x7f8))
free(h1, p2)
p3 = assert(alloc(h1, 0x30))
p4 = assert(alloc(h1, 0xfe8))
free(h1, p4)
p5 = assert(alloc(h1, 0xfe8))
free(h1, p1)
free(h1, p3)
p6 = assert(alloc(h1, 0xfe8))
free(h1, p5)
free(h1, p6)
